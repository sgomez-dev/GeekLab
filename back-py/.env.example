# Base de datos (SQLite por defecto; cambiar a postgresql://user:pass@host/db para Postgres)
DATABASE_URL=sqlite:///./geeklab.db

# JWT
JWT_SECRET=cambia-este-secreto-en-produccion
JWT_ALGORITHM=HS256
JWT_EXPIRE_HOURS=2

# Servidor
HOST=0.0.0.0
PORT=8000

# CORS — orígenes permitidos separados por coma
CORS_ORIGINS=http://localhost:5173,http://localhost:3000,http://127.0.0.1:5173
